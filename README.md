# Image_Captioning_ViT
A deep learning-based image captioning system that combines Swin Vision Transformer (Swin-ViT) as the visual encoder and GPT-2 as the language decoder to generate natural language descriptions of images.
